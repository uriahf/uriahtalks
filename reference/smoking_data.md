# Return a presentation-specific view of the smoking data

Return a presentation-specific view of the smoking data

## Usage

``` r
smoking_data(view = c("observational", "experimental", "mediation", "full"))
```

## Arguments

- view:

  One of `"observational"`, `"experimental"`, `"mediation"`, or
  `"full"`.

## Value

A tibble.
